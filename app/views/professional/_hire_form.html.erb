
    <a href="#" class="button small gradient black">Add favorite</a>
    <div id="follow_form">
      <% if current_user.following?(@professional) %>
          <%= render 'unhire' %>
      <% else %>
          <%= render 'hire' %>
      <% end %>
    </div>
